# Optional tooling for local verification (not needed to run the app).
# Headless browser for manual UI checks:
playwright
# wc2026_pitches.py renders the estimated-XI pitch PNGs (build-time only; the
# app serves the generated images as static assets):
mplsoccer
pytest-asyncio
